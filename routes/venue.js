const express = require('express');
const router = express.Router();

router.get('/venue',function (req,res){
});
router.get('/venue/:id',function (req,res){
});
router.get('/venue/:id/edit',function (req,res){
});
module.export = router;